import { Check, X } from 'lucide-react';
const traditional = ['Built by individuals', 'Permissions added later', 'Code-first', 'Hard to discover', 'Breaks when creator leaves'];
const commons = ['Built by the team', 'Ownership from the start', 'AI-powered, no code needed', 'Easy to discover and share', 'Lives on, no matter what'];
const docs = ['Not built for software', 'Static, not executable', 'No AI to build with you', 'Can’t deploy or run', 'Not a system of record'];
function List({ title, entries, good = false }: { title: string; entries: string[]; good?: boolean }) { return <div className={`rounded-xl p-5 ${good ? 'border border-indigo-300 bg-white shadow-sm' : ''}`}><h3 className={`mb-2 text-sm font-semibold ${good ? 'text-ink' : 'text-slate-700'}`}>{title}</h3><ul>{entries.map((entry) => <li className="flex items-center justify-between border-t border-slate-200 py-2.5 text-sm text-slate-600" key={entry}>{entry}{good ? <Check className="h-4 w-4 rounded-full bg-indigo-500 p-0.5 text-white" /> : <X className="h-4 w-4 text-slate-400" />}</li>)}</ul></div> }
export function Comparison() { return <section className="mx-auto max-w-6xl px-6 py-12 sm:py-16"><div className="rounded-2xl bg-[#F8FAFF] px-5 py-8 sm:px-10 sm:py-10"><h2 className="text-center text-2xl font-bold tracking-tight text-ink">Not just another app builder</h2><div className="mt-7 grid gap-5 lg:grid-cols-3"><List title="Traditional builders" entries={traditional} /><List title="commons." entries={commons} good /><List title="Docs & design tools" entries={docs} /></div></div></section>; }
